[general]
static=yes
writeprotect=no

exten => _X.,1,Stasis(node)
same => n,Hangup()

[from-external]
exten => _X.,1,Stasis(node)
 same => n,Hangup()

[outgoing]
exten => _X.,1,Stasis(node)
 same => n,Hangup()

[from-twilio]
exten => _X.,1,Stasis(node)
 same => n,Hangup()
